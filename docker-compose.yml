services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////app/data/alerts.db
    volumes:
      - threat_detection_data:/app/data

volumes:
  threat_detection_data:
